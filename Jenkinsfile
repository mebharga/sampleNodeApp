
pipeline {
    agent { docker { image pipeline {     agent { docker { im 'node:6.10.1' } }
    stages {
        stage('build') {
            steps {
                sh 'npm --version'
            }
        }
    }
}